{
  "name": "cbp-v2",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev":    "wrangler dev --local",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put SECRET",
    "tail":   "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.57.0"
  }
}
